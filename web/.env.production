# The hosted upload API. Render adds a suffix when a service name is taken,
# so this is the service's actual address, not the name in render.yaml.
VITE_UPLOAD_API_URL=https://supplier-intelligence-api-zfcf.onrender.com
