# URL of the upload API (api/main.py). Leave unset in development: Vite proxies /api
# to http://localhost:8000. For a deployed build, point it at the hosted API, e.g.
# VITE_API_URL=https://supplier-intelligence-api.onrender.com
VITE_API_URL=
