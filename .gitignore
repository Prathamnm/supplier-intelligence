__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
.DS_Store
.env

# Web build artefacts. web/src/data and web/public/briefs ARE committed:
# the hosted build runs only `npm run build`, not the Python pipeline.
web/node_modules/
web/dist/

# Regenerated by `python -m pipeline.run`; the large per-order CSV is not needed in git.
output/orders_with_losses.csv

# Synthetic evaluation datasets (regenerated by python -m evaluation.run)
evaluation/runs/
evaluation/run.log
