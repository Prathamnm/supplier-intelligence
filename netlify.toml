# Netlify: static build of the web app. The Python pipeline is run locally
# and its outputs (web/src/data, web/public/briefs) are committed.
[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Vite 7 needs Node 20.19+.
  NODE_VERSION = "22"
